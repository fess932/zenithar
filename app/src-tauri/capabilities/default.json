{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the Zenithar shell window. External links + browser-open are handled natively via the on_navigation bridge (no frontend IPC), so only core + deep-link are granted here.",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
