// Fingerprinted dotLottie WASM URL — the filename carries the package version so
// it can be cached IMMUTABLY (a new version = a new name = automatic cache bust).
// Single source of truth: build.ts copies the wasm to this name, and
// Sticker.svelte points `DotLottie.setWasmUrl` here. Bump on dep upgrade.
export const DOTLOTTIE_WASM_URL = "/assets/dotlottie-player-0.75.0.wasm";
